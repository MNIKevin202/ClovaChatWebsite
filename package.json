{
  "name": "clovachat-website",
  "version": "1.0.0",
  "private": true,
  "description": "ClovaChat website with customer accounts and admin license tools.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check auth.js && node --check signup.js && node --check account.js && node --check admin.js && node --check admin-setup.js && node --check script.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mongodb": "^7.4.0"
  }
}
